{
  "name": "time-chart",
  "scripts": {
    "watch": "cross-env NODE_ENV=development parcel ./index.html --no-hmr --open"
  },
  "devDependencies": {
    "cross-env": "^6.0.3",
    "parcel": "^1.12.4",
    "typescript": "^3.7.5"
  }
}